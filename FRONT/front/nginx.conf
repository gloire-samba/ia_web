server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Si un utilisateur rafraîchit la page, Nginx le redirige vers l'application Angular
        try_files $uri $uri/ /index.html;
    }
}